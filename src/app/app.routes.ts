import { Component } from '@angular/core';
import { Routes } from '@angular/router';
 
import { MyWatch } from './my-watch/my-watch';

export const routes: Routes = [
    { path: '', component: MyWatch }
];
